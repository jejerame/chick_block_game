# 🚀 Cursor로 가져가기 — 초보자용 순서 가이드

> 이 문서 하나만 따라 하면 됩니다. **Cursor를 처음 켰을 때 이 파일을 먼저 열어서 읽히세요.**

---

## STEP 0. 큰 그림 (1분)

- 이 프로젝트는 **빌드(컴파일) 없이 그냥 `index.html`만 열면 돌아가는** 구조예요.
  (브라우저가 `.jsx` 를 즉석에서 변환 → 설치할 게 없음)
- 그래서 Cursor에 옮길 때 **"프로젝트 폴더 전체"를 그대로 복사**하면 끝입니다.
- 복잡한 환경설정(npm, node…)은 **지금 당장은 필요 없어요.** 나중에 앱을 더 키우고 싶을 때만 고민하면 됩니다 (STEP 5 참고).

---

## STEP 1. 파일 다운로드 — "전체 다운로드"가 정답

부분만 골라 받다 빠뜨리면 안 돌아갑니다. **프로젝트 전체를 zip으로 한 번에** 받으세요.
(이 채팅 아래에 다운로드 카드를 띄워 두었습니다 → 클릭해서 zip 저장)

zip 안에 들어있는 것 중 **반드시 있어야 하는 핵심 파일**은 이거예요:

| 꼭 필요 | 파일 |
|---|---|
| 진입점 | `index.html` |
| 로직 | `app.jsx`, `components.jsx`, `screens.jsx`, `game-data.jsx` |
| 스타일 | `styles.css` |
| 부품(수정금지) | `ios-frame.jsx`, `tweaks-panel.jsx` |
| 그림 | `assets/` 폴더 전체 (병아리 PNG들) |
| 📘 설명서 | `CLAUDE.md`, `HANDOFF_blocks.md`, `handoff/` 폴더 (01~10) |

**없어도 되는 것** (참고용 백업이라 무거우면 지워도 됨):
- `classic/`, `v1_*` → 옛날 버전 보존본
- `standalone_src.html`, `_standalone_*`, `병아리 *.html` → 배포용 단일파일 사본

> 💡 헷갈리면 그냥 **전체 zip 통째로** 받으세요. 위 "없어도 되는 것"까지 들어있어도 작동에는 전혀 지장 없습니다.

---

## STEP 2. Cursor에서 열기

1. zip 압축 풀기 → 폴더 하나 생김
2. Cursor 실행 → **File ▸ Open Folder** → 그 폴더 선택
3. 왼쪽 파일 목록에 `index.html`, `app.jsx`… 가 보이면 성공

---

## STEP 3. 미리보기로 돌려보기

`index.html` 은 더블클릭만으로도 열리지만, 안정적으로 보려면 **로컬 서버**로 띄우는 게 좋아요.

- 가장 쉬운 방법: Cursor 확장 **"Live Server"** 설치 → `index.html` 우클릭 → **Open with Live Server**
- 화면에 아이폰 프레임 + 병아리 블록 가계부가 뜨면 정상.

---

## STEP 4. Cursor(AI)에게 건넬 첫 메시지 — 복붙용

Cursor 채팅창에 아래를 그대로 붙여넣으세요:

```
이 프로젝트를 이어서 작업할 거야. 시작하기 전에 반드시 아래 순서로 읽어줘:
1) CLAUDE.md — 프로젝트 전체 규칙과 파일 구조
2) handoff/ 폴더의 01~10 마크다운 — 각 기능의 확정 사양 (코드와 충돌하면 handoff가 우선)
3) HANDOFF_blocks.md — 블록=거래 데이터 규칙

규칙 요약:
- "테트리스" 단어 금지 → "블록"
- index.html 은 가벼운 셸. 로직은 app.jsx / components.jsx / game-data.jsx 에서만 수정
- 큰 리팩토링 전에는 나에게 먼저 확인
- ios-frame.jsx, tweaks-panel.jsx 는 건드리지 마

다 읽었으면, 지금 구현된 것과 아직 안 된 것(CLAUDE.md "향후 작업 후보")을 정리해서 보여줘.
```

> 핵심: **Cursor는 `CLAUDE.md` 파일을 자동으로 프로젝트 규칙으로 인식**합니다. 그래서 위처럼 "읽어줘"라고만 해도 맥락을 거의 다 잡아요.

---

## STEP 5. (나중에) 진짜 앱으로 키울 때만

지금은 필요 없지만, 앱이 커지면 Cursor가 "Vite로 옮기자"고 제안할 수 있어요. 그때 이렇게 부탁하면 됩니다:
```
이 프로토타입을 Vite + React 프로젝트로 마이그레이션해줘.
지금은 브라우저 Babel로 .jsx 를 즉석 변환 중인데, 정식 빌드 구조로 바꾸고 싶어.
디자인/로직은 그대로 유지하고 구조만 바꿔줘.
```

---

## ✅ 기절 모드 체크 (질문 주신 것)

이미 **완성된 상태로 함께 넘어갑니다**:
- 자동: 블록이 목표선 위로 쌓이면 자동 발동 (`app.jsx` `setGhostMode(true)`)
- 수동: Tweaks 패널 `👻 기절 모드 (목표선 초과)` 토글
- 해제: 저축 줄 비우기(`onRescue`) 시 꺼짐
- 합산: `effectiveGhost = ghostMode || tweaks.ghostPreview`

Cursor에게 "기절 모드 트리거 더 다듬어줘" 라고 하면 발동 시점/연출만 손보면 됩니다.
